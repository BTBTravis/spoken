service: todo

provider:
  name: aws
  runtime: nodejs4.3

functions:
  echo:
    cljs: todo.core/echo
    events:
      - http:
          path: echo
          method: post

plugins:
  - serverless-cljs-plugin
